services:
  enhanced-registration-bridge:
    build: .
    container_name: enhanced-registration-bridge
    restart: unless-stopped
    env_file:
      - .env
    ports:
      - "127.0.0.1:18081:18080"
